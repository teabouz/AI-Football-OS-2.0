python-telegram-bot==21.6
SQLAlchemy==2.0.35
anthropic==0.39.0
python-dotenv==1.0.1
flask==3.0.3
# httpx is pinned below 0.28 because anthropic==0.39.0's internal client
# construction breaks on httpx>=0.28 (it passes a `proxies` kwarg httpx
# removed). Don't let this float up on a routine `pip install --upgrade`.
httpx==0.27.2
requests==2.32.3
Pillow==12.1.1
alembic==1.19.1

# Phase 4 (video analysis) also requires the ffmpeg + ffprobe SYSTEM binaries
# on PATH — not a pip package, so it's not listed here. Install separately:
#   Ubuntu/Debian: sudo apt install ffmpeg
#   macOS:         brew install ffmpeg
